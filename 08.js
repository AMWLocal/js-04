//Написать функцию, которая принимает от 1 до 5 чисел и возвращает большее из них.
function max(num1, num2, num3, num4, num5) {
    return Math.max(num1, num2, num3, num4, num5)
}
let res = max(1, 8, 9, 15, 18);
console.log(res)